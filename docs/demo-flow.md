# Demo Flow

The demo simulates traffic spikes and prompt growth. It runs two passes:
- baseline (no control)
- with control (policy + decisions enabled)

It prints outcomes, metrics snapshots, and derived signals.
